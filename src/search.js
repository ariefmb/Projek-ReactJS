// import { searchMovie } from "./api";
// import PopularMovieList from "./hook";

// const Search = async (q) => {
//     if (q.length > 3) {
//         const query = await searchMovie(q);
//         <PopularMovieList set={setMovies(query.result)}/> 
//     }
// }

// export default Search